# Annotate `CLAUDE.md` enforcement rules with stage classification

Added classification tables after each section of CLAUDE.md, covering all development discipline rules. 24 rules classified as temporary (16 Stage 1, 8 Stage 2) and 19 rules classified as permanent. Each temporary rule names its replacement stage and specific replacement mechanism (CLI command, hook policy, graph engine validation, etc.). Format: summary table per section with columns Rule, Classification, Stage, Replacement.
