tag_prefix = "v"

[changelog]
path = "CHANGELOG.md"
template = "remote"
remote = "github.com"
repository = "arci"
owner = "tbhb"
authors = [{ username = "tbhb", signature = "Tony Burns" }]

[commit_types]
arci = { omit_from_changelog = true }
chore = { omit_from_changelog = true }
test = { omit_from_changelog = true }
