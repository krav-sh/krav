# Daemon (moved)

The [server design](server/index.md) supersedes this document. The ARCI daemon is now the ARCI server. See also [MCP server](mcp/index.md) for the MCP integration, which now has its own documentation.
